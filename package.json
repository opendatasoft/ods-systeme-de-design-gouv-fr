{
  "name": "ods-systeme-de-design-gouv-fr",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npx sass sass/:export/ --watch",
    "build": "npx sass --no-source-map sass/:export/ && npx postcss export/main.css > export/ods-dsfr.min.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Opendatasoft",
  "license": "MIT",
  "dependencies": {
    "@gouvfr/dsfr": "^1.6.0",
    "cssnano": "^5.1.12",
    "sass": "^1.53.0",
    "postcss": "^8.4.14",
    "postcss-cli": "^10.0.0"
  }
}
